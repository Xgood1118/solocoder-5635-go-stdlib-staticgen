有几个问题：
1. build 命令运行直接报错退出，根本生成不出 index.html 等任何 HTML 文件。
2. dev 命令也起不来，启动时初始构建阶段就报同样错误退出，连开发服务器都没启动。
3. deploy 命令构建阶段就崩了，目标目录一个静态文件都复制不过去。
4. 报错信息是 base 模板的 title 标签里访问字段时崩溃，所有用 base 继承的页面都连带失败。
检查修复一下。